//Tenemos 3 tipos de variables

const incambiale = "los valores const no pueden cambiar nunca"

let variable = "los valores let pueden ser cambiados lineas abajo"
let lowerCamelCase = "a este lo llamamos asi ya que cuando la variable es larga emepzamos en min y cada palabra es una mays"

var vieja = "esta es la forma menos recomendable de variable"

//variables cambiantes

let valorEstable = "esto es el texto original"
console.log(valorEstable) //output [esto es el texto original]

valorEstable = "Esto es el segundo texto"
console.log(valorEstable)/*output
    [esto es el texto original]
    [Esto es el segundo texto]
 */

    //nos genera las 2 variables 